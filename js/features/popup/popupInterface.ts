export interface PopupInterface {

}

export type PopupOptions = {
    offset?: [number, number],
    maxWidth?: number,
    maxHeight?: number,
    className?: string,
    closeButton?: boolean,
    keepInView?: boolean
}